[workspace]
members = [
  "dune",
  "dune_lib",
  "dune_data_gen"
]
default-members = ["dune"]

[profile.dev]
panic = "abort"

[profile.release]
debug = true
panic = "abort"

[profile.release-lto]
inherits = "release"
lto = true
