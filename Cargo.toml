[workspace]
members = [
  "dune",
  "dune_lib",
  "dune_common",
  "dune_data",
  "dune_data_gen"
]
default-members = ["dune"]

[profile.dev]
panic = "abort"

[profile.release]
debug = true
panic = "abort"
lto = "thin"

[profile.release-lto-fat]
inherits = "release"
lto = "fat"
